{
  "name": "freeflix-website",
  "version": "1.0.0",
  "description": "FreeFlix Movie Streaming Website",
  "scripts": {
    "dev": "bunx --bun serve -p 3000 --cors -s .",
    "start": "npx http-server -a 0.0.0.0 -p 3000 -c-1",
    "build": "mkdir -p dist && cp -r ./* ./dist/ && rm -rf ./dist/dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
